from setuptools import setup, find_packages

setup(
    name="tactile_spiking_neural_network",
    version="0.1",
    packages=find_packages(),
    install_requires=[
        'numpy',
        'pytest'
    ]
) 